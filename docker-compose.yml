services:
  mysql:
    image: mysql:8.0
    container_name: lantern_DB
    restart: always
    environment:
      MYSQL_DATABASE: lantern
      MYSQL_ROOT_PASSWORD: lantern
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: